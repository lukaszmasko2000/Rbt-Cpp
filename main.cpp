#include <iostream>
#include "rbTree.h"


int main() {

    RedBlackTree<int> rbt;

    rbt.insert(10);
    rbt.insert(20);
    rbt.insert(30);
    rbt.insert(40);
    rbt.insert(50);

    return 0;
}